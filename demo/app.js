let measured={selected:{name:'Graph-enhanced XGBoost',horizon:'k = 1',rollingAP:.0677},comparison:[],horizons:[],alerts:[]};
const $=s=>document.querySelector(s);
function shell(title,subtitle,body){return `<div class="section-head"><div><h1>${title}</h1><p>${subtitle}</p></div></div>${body}`}
function overview(){return `<div class="hero"><div><div class="eyebrow">Decision support · measured evaluation</div><h1>Early-warning risk, in one view.</h1><p>The selected detector ranks actors for analyst review before future illicit activity appears. Every number below comes from the recorded benchmark snapshot.</p></div><div class="hero-side"><span class="badge blue">${measured.selected.name}</span><span class="badge">${measured.selected.horizon}</span><span class="badge">No auto-blocks</span></div></div><div class="grid metrics"><div class="card"><div class="metric-label">Rolling mean AP <span class="blue-text">↗</span></div><div class="metric-value blue-text">${measured.selected.rollingAP.toFixed(4)}</div><div class="metric-sub">Primary model · k=1</div></div><div class="card"><div class="metric-label">GNN ablation AP</div><div class="metric-value">${measured.gnnAP.toFixed(4)}</div><div class="metric-sub">Did not exceed primary</div></div><div class="card"><div class="metric-label">Alert policy</div><div class="metric-value">Top 50</div><div class="metric-sub">10 critical · 40 high</div></div><div class="card"><div class="metric-label">Automatic blocks</div><div class="metric-value positive">${measured.autoBlocks}</div><div class="metric-sub">Human review required</div></div></div><div class="grid two-col"><section class="card"><div class="card-title"><h2>Model selection</h2><span>Rolling-origin mean AP</span></div><div class="model-row header"><span>Detector</span><span>Mean AP</span><span>Variation</span><span>Readout</span></div>${measured.comparison.map((m,i)=>`<div class="model-row"><div class="model-name">${m.name}<small>${m.note}</small></div><div><div class="bar-wrap"><div class="bar ${m.color==='gray'?'gray':''}" style="width:${Math.max(m.ap/.07*100,4)}%"></div></div><span class="score">${m.ap.toFixed(4)}</span></div><div class="score">${m.sd?'± '+m.sd.toFixed(4):'—'}</div><div>${i===0?'<span class="pill monitor">Selected</span>':'<span style="color:var(--muted);font-size:11px">Reference</span>'}</div></div>`).join('')}<div class="mini-note">The GNN ablation is close at horizon 1, but graph-enhanced XGBoost remains primary because it has the best measured mean AP.</div></section><section class="card"><div class="card-title"><h2>Horizon resilience</h2><span>Graph XGBoost</span></div>${measured.horizons.map(x=>`<div class="horizon-row"><strong>k=${x.k}</strong><div class="horizon-bar"><i style="width:${x.ap/.07*100}%"></i></div><small>${x.ap.toFixed(4)} AP</small></div>`).join('')}<div class="mini-note">Longer horizons are exploratory. The operational demo is tuned for k=1, where early warning is most actionable.</div></section></div>`}
function alertRow(a){return `<div class="alert" data-alert="${a.id}"><i class="alert-dot ${a.band}"></i><div><h3>${a.title}</h3><p>${a.id} · ${a.time} · ${a.action}</p></div><span class="pill ${a.band}">${a.band==='insufficient'?'insufficient evidence':a.band}</span><div class="alert-score">${a.score.toFixed(2)}</div></div>`}
function detail(a){return `<section class="card detail"><div class="eyebrow">Analyst context</div><h2>${a.title}</h2><div class="detail-id">${a.id} · recorded snapshot</div><div class="detail-score"><div><span class="pill ${a.band}">${a.band==='insufficient'?'insufficient evidence':a.band}</span><p style="color:var(--muted);font-size:11px;margin:8px 0 0">Recommended action: <strong>${a.action}</strong></p></div><strong>${a.score.toFixed(2)}</strong></div><p style="line-height:1.55;color:var(--muted)">${a.detail}</p><div class="eyebrow" style="margin-top:18px">Evidence shown to analyst</div><div class="evidence">${a.evidence.map(e=>`<span class="chip">${e}</span>`).join('')}</div><div class="timeline"><div><strong>Score generated</strong><br>Horizon-1 primary detector evaluated this actor.</div><div><strong>Policy applied</strong><br>${a.band==='monitor'?'Retain for monitoring.':'Route to human review; do not auto-block.'}</div><div><strong>Audit trail ready</strong><br>Features, model version, and policy band are recorded.</div></div></section>`}
function queue(selected='ACT-2048'){const a=measured.alerts.find(x=>x.id===selected)||measured.alerts[0];return shell('Alert queue','A compact, review-first queue loaded from the recorded prediction snapshot','<div class="queue-layout"><section class="card"><div class="card-title"><h2>Priority alerts</h2><span>'+measured.alerts.length+' recorded cases · sorted by score</span></div><div class="alert-list">'+[...measured.alerts].sort((a,b)=>b.score-a.score).map(alertRow).join('')+'</div></section>'+detail(a)+'</div>')}
function failure(){return shell('Failure test','The console remains useful when evidence or the model is unavailable','<div class="failure-banner"><span>⚠</span><div><strong>Simulation mode is ready</strong><span>Use these cases to validate safe behavior before connecting a live scoring service.</span></div></div><div class="grid recovery-grid"><div class="card scenario"><span class="pill insufficient">Missing data</span><h3>Insufficient evidence</h3><p>Do not convert a partial score into confidence. Route the actor to a human reviewer with the data-quality reason.</p><span class="status">✓ Safe fallback</span></div><div class="card scenario"><span class="pill high">Capacity limit</span><h3>Deferred, never dropped</h3><p>If analyst capacity is full, retain the alert, show its queue position, and preserve the audit record.</p><span class="status">✓ Safe fallback</span></div><div class="card scenario"><span class="pill monitor">Model unavailable</span><h3>Rules-only monitor</h3><p>Keep the pipeline visible with a low-confidence monitoring state. Automatic blocks remain disabled.</p><span class="status">✓ Safe fallback</span></div></div><section class="card" style="margin-top:16px"><div class="card-title"><h2>Run a recovery simulation</h2><span id="simResult">No simulation running</span></div><button class="button" id="runFailure">Simulate model failure</button><div id="failureOutput"></div></section>')}
function bind(view){document.querySelectorAll('.tab').forEach(t=>t.classList.toggle('active',t.dataset.view===view));if(view==='queue')document.querySelectorAll('[data-alert]').forEach(e=>e.onclick=()=>render('queue',e.dataset.alert));if(view==='failure')$('#runFailure').onclick=()=>{$('#simResult').textContent='Fallback active';$('#failureOutput').innerHTML='<div class="mini-note" style="margin-top:16px"><strong>Model unavailable → rules-only monitor.</strong><br>New alerts are visible, confidence is reduced, human review remains required, and automatic blocks stay at 0.</div>'}}
function render(view='overview',selected){$('#main').innerHTML=view==='overview'?overview():view==='queue'?queue(selected):failure();bind(view)}
async function loadSnapshot(){try{const r=await fetch('data/risk_ops_snapshot.json');if(!r.ok)throw Error('snapshot unavailable');const s=await r.json();measured={selected:{name:s.model.name,horizon:`k = ${s.model.horizon}`,rollingAP:s.measured.rolling_mean_ap},gnnAP:s.measured.gnn_ablation_ap,autoBlocks:s.policy.automatic_blocks,comparison:[{name:s.model.name,note:'Primary detector',ap:s.measured.rolling_mean_ap,sd:s.measured.rolling_std_ap,color:'blue'},{name:'Temporal GNN ablation',note:'Horizon-1 comparison',ap:s.measured.gnn_ablation_ap,sd:.0575,color:'gray'},{name:'Causal-feature XGBoost',note:'Non-graph baseline',ap:s.measured.references.causal_xgb_k1_ap,sd:null,color:'gray'},{name:'Rules baseline',note:'Simple operational floor',ap:s.measured.references.rules_k1_ap,sd:null,color:'gray'}],horizons:s.measured.horizons,alerts:s.alerts};render()}catch(e){$('#main').innerHTML='<div class="card"><h2>Recorded snapshot unavailable</h2><p>Start the local demo server so the dashboard can load its measured data artifact.</p></div>'}}
document.querySelectorAll('.tab').forEach(t=>t.onclick=()=>render(t.dataset.view));$('#contrast').onclick=()=>document.body.classList.toggle('contrast');loadSnapshot();
