{
  "name": "razp-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "RazP Sentinel - Autonomous Zero-Loss Payment Recovery Engine",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
